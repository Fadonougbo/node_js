<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><% if (locals.title) { %>
            <%= title %>
          <% } else { %>
            Document
          <% } %>
    </title>
</head>
<body>
<header>
  <nav>
    <a href="/">Home</a>
    <% if (locals.request && locals.request.session.get("admin")) { %>
      <a href="/admin">Admin</a>
      <a href="/logout">Deconnection</a>
    <% } else { %>
      <a href="/login/dashboad">Connection</a>
    <% } %>
  </nav>
</header>
    